import { DirScrollAnimationDirective } from './dir-scroll-animation.directive';

describe('DirScrollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new DirScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
